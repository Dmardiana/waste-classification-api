{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run FastAPI",
      "type": "debugpy",
      "request": "launch",
      "module": "uvicorn",
      "args": [
        "app:app",
        "--reload"
      ],
      "cwd": "${workspaceFolder}",
      "jinja": true,
      "python": "${workspaceFolder}/venv/bin/python"
    }
  ]
}